const initialState = {
  posts: [], //array
  post: {}, //object
  loading: false
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
